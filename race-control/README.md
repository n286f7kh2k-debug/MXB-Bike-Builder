# MXB Race Control update channel

This folder is reserved for the MXB Race Control app's one-click updater. `latest.json` is the permanent update manifest used by v0.1.0 and later. Future release ZIPs will be placed under `race-control/releases/` and the manifest will be updated with the hosted file's SHA-256 checksum.
